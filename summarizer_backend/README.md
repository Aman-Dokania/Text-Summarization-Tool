# Summarizer Backend

A FastAPI backend for text summarization using NLTK.

## Setup

### 1. Install dependencies:
   ```bash
pip install -r requirements.txt
   ```

### 2. Run the server:
   ```bash
uvicorn main:app --reload
```

### The backend will be available at 
```bash
http://localhost:8000
```
